CORRECTED
1. Original: "A Zero-Trust policy enforcement mechanism uses a rule engine to continuously verify UAV sensor communications (allowed ports, endpoints, protocols) against rules encoded and stored as smart-contract policies on the blockchain ledger."
   Paper says: "The rules and permissions that govern the communication (such as allowed ports and endpoints) are encoded as Zero Trust security rules and stored in the blockchain ledger." — "protocols" is not part of the ZT rule attributes described in the paper.
   Changed to: "A Zero-Trust policy enforcement mechanism uses a rule engine to continuously verify UAV sensor communications (such as allowed ports and endpoints) against rules encoded and stored on the blockchain ledger."
