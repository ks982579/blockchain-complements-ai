CORRECTED

- Overview: "cross-chain atomic swaps" changed to "cross-chain payments." The paper (Section 11, "Cross-Chain Payments") describes cross-chain payment routing using compatible hash functions but never uses the term "atomic swaps." Original text: "cross-chain atomic swaps, and financial smart contracts." Corrected to: "cross-chain payments, and financial smart contracts."

- Key Points (cross-chain bullet): "enabling atomic swaps where neither the sender nor receiver needs to understand the other chain's consensus rules" changed to "enabling payments to be routed across chains where neither the sender nor receiver needs to understand the other chain's consensus rules." The paper (Section 11) says "it's possible for transactions to be routed over multiple chains" and does not use the term "atomic swaps." Additionally, "compatible hash functions" was changed to "similar hash functions" to match the paper's wording ("similar hash-functions across chains"). Original text: "Cross-chain payments are possible if different blockchains share compatible hash functions, enabling atomic swaps where neither the sender nor receiver needs to understand the other chain's consensus rules." Corrected to: "Cross-chain payments are possible if different blockchains share similar hash functions, enabling payments to be routed across chains where neither the sender nor receiver needs to understand the other chain's consensus rules."
